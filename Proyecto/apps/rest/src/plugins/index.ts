// Plugins de Fastify (auth, rate-limit, etc.) registrados desde app.ts.
// "auth" (requireAuth/requireRol) es el ejemplo de referencia; el resto
// (rate-limit...) se agrega siguiendo el mismo patron.
export * from "./auth.js";
