# shared

Componentes, pipes y directivas de UI reutilizables entre features (ej.
tarjeta de producto, selector de distribuidor, badge de calificación). Sin
lógica de negocio ni llamadas a la API — eso vive en cada feature.
