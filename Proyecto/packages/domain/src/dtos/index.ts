// DTOs y schemas de validación (zod) para entrada/salida de casos de uso.
// "categoria" es el ejemplo de referencia; el resto (CreateOfertaDto,
// AddCarroLineaDto, CheckoutOrdenDto...) se agrega siguiendo el mismo patron.
export * from "./categoria.dto.js";
